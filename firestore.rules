rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for everything - this is a development/demo project
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
